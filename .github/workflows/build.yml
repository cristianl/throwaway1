name: Probe1

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macOS-latest]
        include:
          - os: macOS-latest

    steps:
      - run: echo "hi";
          echo "hi2";
          echo "hi3";
          echo "hi4";
